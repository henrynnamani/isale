import React from 'react';

const ProductCategory = ({ label }: { label: string }) => {
  return (
    <div>
      <span className="font-semibold text-xl">Discount Products</span>
      <div></div>
    </div>
  );
};

export default ProductCategory;
